{
  "_about": [
    "Configuration for scripts/governance_docs.py (DOC-G1, DOC-G2, DOC-G4, DOC-G5, DOC-G6).",
    "Copy this file to docs/governance/docs-policy.json and edit it. The copy is",
    "project-owned: the harness never manages it, so your edits are not drift.",
    "Keys starting with _ are ignored by the gate.",
    "",
    "link_roots        DOC-G1: files or directories whose relative Markdown links must resolve.",
    "metadata_roots    DOC-G2: directories whose Markdown must carry lifecycle frontmatter.",
    "legacy_missing_frontmatter_max  DOC-G2 ratchet: how many files may still lack frontmatter.",
    "                  Lower it as you migrate; never raise it.",
    "seal_roots        DOC-G4: git pathspec limiting the sealed-event immutability check.",
    "portability_roots DOC-G5: directories that must not contain machine-bound home paths.",
    "environment_owner DOC-G5: the one file allowed to name absolute home paths.",
    "spec_homes        DOC-G6: every candidate specification home in this repository.",
    "                  An empty list skips the gate.",
    "spec_home         DOC-G6: the one home that owns present-tense requirement truth.",
    "                  null means undeclared: the gate then only refuses two populated homes.",
    "",
    "evidence_journal      EVID-G: append-only record of what was actually run.",
    "requirements_source   EVID-G: file or directory holding requirement ids.",
    "                      Defaults to spec_home when unset.",
    "requirement_pattern   EVID-G: regex with one capture group naming a",
    "                      requirement. Leave unset to skip the gate. Examples:",
    "                        OpenSpec  ^###\\\\s+Requirement:\\\\s*(.+?)\\\\s*$",
    "                        numeric   ^###\\\\s+([0-9]+\\\\.[0-9]+)\\\\b",
    "                      A completion claim without a matching fresh record is",
    "                      refused; evidence is bound to the commit it ran at, so",
    "                      it goes stale the moment the code changes.",
    "",
    "POLICY-G (scripts/verify-policies.py) compares this copy against the",
    "example the release ships. When a release adds a key here, your copy does",
    "not gain it — the harness never edits a project-owned file — so the gate",
    "reading it would treat the capability as unconfigured and report clean.",
    "POLICY-G turns that silence into a finding.",
    "To decline a key deliberately, list its name in \"_declined\": [\"key\"].",
    "Declining is a decision and leaves a trace; not knowing is neither."
  ],
  "schema_version": 1,
  "link_roots": [
    "docs/governance/rules",
    ".agents/skills"
  ],
  "link_excludes": [],
  "metadata_roots": [
    "docs/governance/rules"
  ],
  "metadata_excludes": [],
  "legacy_missing_frontmatter_max": 0,
  "seal_roots": [
    "docs"
  ],
  "portability_roots": [
    "docs/governance/rules"
  ],
  "portability_excludes": [],
  "environment_owner": "AGENTS.md",
  "spec_homes": [
    "openspec/specs",
    ".kiro/specs",
    "requirements",
    "spec"
  ],
  "spec_home": null,
  "evidence_journal": ".evidence.jsonl",
  "requirements_source": null,
  "requirement_pattern": null
}
